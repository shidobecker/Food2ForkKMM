pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "Food2ForkKMM"
include(":androidFoodToForkApp")
include(":shared")